# -- Pepper -- Versoes fixas (Opcao A) -----------------------------------------
# Testadas e validadas em 29/05/2026 -- Python 3.14.5, Windows 11.
# Streamlit Cloud: usar Python 3.12 (3.14 incompativel com algumas extensoes nativas)
# Para atualizar: testar localmente, validar app.py, e editar as versoes abaixo.
# Instalar: pip install -r requirements.txt
#
# 29/05/2026 (builder): sincronizado com o ambiente em uso (pip freeze do venv).
# 03/06/2026: supabase fixado em 2.7.4 para evitar pyiceberg (crashava no cloud)
# ------------------------------------------------------------------------------

streamlit==1.58.0
pandas==3.0.3
numpy==2.4.6
plotly==6.7.0
requests==2.34.2
urllib3==2.7.0

# PDF (carta de solicitacao Microvix, exports futuros)
reportlab==4.5.1
pypdf==6.12.2

# Excel / parsing
openpyxl==3.1.5
xlrd==2.0.2

# Seguranca / crypto (NF-e SEFAZ)
cryptography==48.0.0

# XML / HTML parsing
lxml==6.1.1

# Cloud storage (Supabase - usado quando modo_cloud=True no Streamlit Cloud)
# Fixado em 2.7.4: versoes >= 2.8 puxam pyiceberg que crasha no Python 3.12/Linux
supabase==2.7.4
